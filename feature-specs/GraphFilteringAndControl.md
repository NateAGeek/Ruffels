# Goal: Graph Filtering And Control

We need some more controls to view and edit the graph. This should be a WISYWIG like topbar at the top of the graph. We want to be able to get a list of all the node types, and filter them down. We also should support searching the grid for specific types, control what files we focus.
